name: Protect PocketBase data and PB_SEED

# Disabled from automatic runs — will not block any push or PR
on:
  workflow_dispatch:

jobs:
  guard-pb-data:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "This workflow is disabled from automatic runs. Trigger manually if needed."
