# Railway config for React/Vite web frontend
[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build:railway"
watchPaths = ["apps/web/**"]

[deploy]
startCommand = "npm run start:railway"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
