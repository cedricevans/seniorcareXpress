# Railway config for Express API service
[build]
builder = "NIXPACKS"
buildCommand = "npm install"
watchPaths = ["apps/api/**"]

[deploy]
startCommand = "node src/main.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
